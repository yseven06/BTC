#!/usr/bin/env sh
cd "$(git rev-parse --show-toplevel)/frontend" || exit 1

echo "▸ lint-staged"
npx --no-install lint-staged || exit 1

echo "▸ typecheck (tsc --noEmit)"
npm run typecheck --silent || exit 1

echo "▸ design-gates (strict)"
npm run lint:design:strict --silent || exit 1
